<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CC² · Café Room</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background: linear-gradient(135deg, #f6c1d1, #c7b7f3);
      color: #2f2f2f;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Top bar */
    .top-bar {
      width: 100%;
      padding: 16px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(255,255,255,0.15);
      backdrop-filter: blur(10px);
    }

    .top-bar h1 {
      font-size: 18px;
      margin: 0;
      font-weight: 600;
    }

    .home-btn {
      background: rgba(255,255,255,0.25);
      border: none;
      padding: 10px 16px;
      border-radius: 999px;
      font-size: 14px;
      cursor: pointer;
    }

    .home-btn:hover {
      background: rgba(255,255,255,0.35);
    }

    /* Main card */
    .card {
      margin-top: 40px;
      width: 92%;
      max-width: 700px;
      background: rgba(255,255,255,0.2);
      border-radius: 26px;
      padding: 28px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.15);
    }

    .card h2 {
      margin-top: 0;
      font-size: 26px;
      text-align: center;
    }

    .card p {
      text-align: center;
      opacity: 0.9;
      margin-bottom: 30px;
    }

    /* Chat area (placeholder for now) */
    .chat-box {
      background: rgba(255,255,255,0.35);
      border-radius: 18px;
      padding: 20px;
      height: 260px;
      overflow-y: auto;
      margin-bottom: 16px;
    }

    .message {
      margin-bottom: 12px;
      padding: 10px 14px;
      background: rgba(255,255,255,0.6);
      border-radius: 14px;
      width: fit-content;
      max-width: 80%;
    }

    .message.you {
      margin-left: auto;
      background: #e9d8ff;
    }

    /* Input */
    .chat-input {
      display: flex;
      gap: 10px;
    }

    .chat-input input {
      flex: 1;
      padding: 14px;
      border-radius: 999px;
      border: none;
      font-size: 14px;
    }

    .chat-input button {
      padding: 14px 18px;
      border-radius: 999px;
      border: none;
      background: #b98af2;
      color: white;
      font-weight: 600;
      cursor: pointer;
    }

    .chat-input button:hover {
      opacity: 0.9;
    }

    footer {
      margin-top: auto;
      padding: 18px;
      font-size: 12px;
      opacity: 0.7;
    }
  </style>
</head>

<body>

  <!-- Top Bar -->
  <div class="top-bar">
    <h1>☕ Enter Café</h1>
    <button class="home-btn" onclick="goHome()">Home</button>
  </div>

  <!-- Main -->
  <div class="card">
    <h2>Welcome to the Café</h2>
    <p>
      A relaxed space to chat, say hello, and ease into friendship.<br />
      No pressure. Just people.
    </p>

    <div class="chat-box" id="chatBox">
      <div class="message">👋 Someone says hello…</div>
      <div class="message you">Hey there ☺️</div>
    </div>

    <div class="chat-input">
      <input type="text" id="chatInput" placeholder="Type something friendly…" />
      <button onclick="sendMessage()">Send</button>
    </div>
  </div>

  <footer>
    Made with love in Eíre 🇮🇪
  </footer>

  <script>
    // simple login guard – DOES NOT log out
    if (localStorage.getItem("cc2_logged_in") !== "true") {
      window.location.href = "login.html";
    }

    function goHome() {
      window.location.href = "index.html";
    }

    function sendMessage() {
      const input = document.getElementById("chatInput");
      const text = input.value.trim();
      if (!text) return;

      const msg = document.createElement("div");
      msg.className = "message you";
      msg.textContent = text;

      document.getElementById("chatBox").appendChild(msg);
      input.value = "";
      document.getElementById("chatBox").scrollTop =
        document.getElementById("chatBox").scrollHeight;
    }
  </script>

</body>
</html>